apiVersion: v1
kind: Service
metadata:
  name: ${KTX_APP_NAME}
spec:
  selector:
    app: ${KTX_APP_NAME}
  ports:
    - protocol: TCP
      port: ${KTX_APP_PORT}
      targetPort: ${KTX_SVC_PORT}
  type: NodePort
